[user]
	name = Jeffrey Heifetz
	email = jeff.heifetz@mavencare.com
	signingkey = 4F8B0AA08AA5B906
[github]
	user = jeffheifetz
[color]
	ui = true
	status = auto
	diff = auto
	interactive = auto
	branch = auto
[core]
	autocrlf = input
	excludesfile = ~/.gitignore
[alias]
    pr = !git log -1 --pretty=%B | awk '{ print \"\\\"\"$0\"\\\"\"}' | xargs hub pull-request -l "NEEDS_REVIEW" -m | xargs open
    find-merge = "!sh -c 'commit=$0 && branch=${1:-HEAD} && (git rev-list $commit..$branch --ancestry-path | cat -n; git rev-list $commit..$branch --first-parent | cat -n) | sort -k2 -s | uniq -f1 -d | sort -n | tail -1 | cut -f2'"
    show-merge = "!sh -c 'merge=$(git find-merge $0 $1) && [ -n \"$merge\" ] && git show $merge'"
    cleanup = "!git branch --merged | grep  -v '\\*\\|master' | xargs -n 1 git branch -d"

[commit]
	gpgsign = true
